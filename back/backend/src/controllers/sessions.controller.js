// Imports
const { userService } = require("../services/index.service");
const { isValidPassword } = require("../utils/bcryptHash");

// Code
class SessionController {
  // login = async (email, password) => await 
}

// Exports
module.exports = new SessionController();